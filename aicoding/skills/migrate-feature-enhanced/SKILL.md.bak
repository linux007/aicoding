---
name: migrate-feature-enhanced
description: |
  模块接口功能迁移 skill。当你需要将某个功能从老模块迁移到新模块实现时使用。
  典型场景：新模块需要实现老模块已有的某个功能，PHP 老实现 → Go 新实现，或 Go 旧实现 → Go 新实现。
  当用户提到功能迁移、模块迁移、接口迁移、从老模块实现新模块、从 A 模块迁移到 B 模块时使用此 skill。
  【核心增强】包含约束性 AI 指令操控（禁止自主重构）、分层分步迁移（外层→中层→内层→底层）、逐行等价验证、全量回归测试保障等完整流程。
  确保使用 brainstorming 先理解迁移目标，openspec 管理 spec.md，tdd 工作流实现。
---

# 模块接口功能迁移 Skill（增强版）

## Concept & Trigger

**触发场景：** 当你需要将某个功能从老模块迁移到新模块实现时使用。

**典型场景：**
- 新模块需要实现老模块已有的某个功能
- 功能迁移：老模块 A 的功能 → 新模块 B
- PHP 老实现 → Go 新实现，或 Go 旧实现 → Go 新实现
- 需要复用老模块的数据源和业务逻辑

**目标用户：** 个人开发者（自用）

## Tech Stack
- 源语言：PHP、Go
- 目标语言：Go
- 遵循目标模块的架构和代码风格

## 核心原则

### 1. 逐行等价直译（禁止自主重构）

迁移时**强制**遵循以下约束：

```
✓ 必须做：
  - 逐行翻译老模块的每一行逻辑
  - 保持相同的变量命名逻辑
  - 保持相同的条件判断顺序
  - 保持相同的循环遍历顺序
  - 保持相同的异常处理分支

✗ 禁止做：
  - 禁止 AI 自主重构代码风格
  - 禁止简化复杂的条件嵌套
  - 禁止合并重复逻辑（除非显式要求）
  - 禁止改变算法实现方式
  - 禁止脑补缺失的依赖关系
```

**为什么重要：** 自主重构会导致业务逻辑偏差，迁移的目标是 100% 等价而非代码优化。

### 2. 分层分步迁移

遵循"由浅入深、逐层验证"原则：

```
迁移顺序（必须严格按此顺序）：
1. 外层入口   → 先迁移调用入口点
2. 中层业务   → 再迁移业务处理逻辑
3. 内层子方法 → 然后迁移被调用的子方法
4. 底层工具   → 最后迁移工具函数

每完成一层，必须验证后再进入下一层。
```

## Workflow

### Phase 1: 迁移前期规划

**必须完成以下步骤才能进入 Phase 2：**

1. **Brainstorming** — 使用 `superpowers:brainstorming` 理解迁移目标
   - 确定源模块（老功能）和目标模块（新实现）
   - 理解功能的输入、输出、依赖关系
   - 识别深层嵌套的调用层级

2. **创建/检查 OpenSpec** — 使用 `/opsx:new` 或检查现有 `spec.md`
   - 记录源模块的接口契约
   - 列出需要迁移的功能点
   - 确认目标模块的接口定义
   - **必须包含**：调用链路层级图、依赖关系表

3. **代码探索** — 按以下优先级调研老模块实现：

   ```
   优先级 1: LSP 工具
   ├── hover <变量名>           → 直接查看 struct 完整定义
   ├── goToDefinition <方法名>   → 跳转到方法实现
   └── findReferences <符号>     → 查找所有引用位置

   优先级 2: Agent Explore
   └── 使用 Explore agent 广泛搜索老模块实现

   优先级 3: Grep
   └── 精确文本匹配（备选）

   优先级 4: Read
   └── 已知文件路径时使用（最后备选）
   ```

4. **抽取核心业务契约**（必须记录到 spec.md）：
   - 方法签名（入参、返回值）
   - 异常处理规则
   - 事务规则
   - 隐式依赖（全局状态、外部调用）

### Phase 2: 实现

1. **分层指令迁移** — 使用约束性 Prompt

   ```markdown
   迁移指令模板（必须使用）：

   ## 当前迁移层：<外层入口/中层业务/内层子方法/底层工具>

   ## 约束规则：
   1. 逐行翻译，不许重构
   2. 保持变量命名对应关系
   3. 保持条件判断顺序
   4. 如遇不确定的隐式依赖，停下来询问

   ## 待迁移代码段：
   [粘贴老模块代码]

   ## 目标位置：
   [新模块文件路径]
   ```

2. **TDD 循环** — 使用 `superpowers:test-driven-development`
   - 先写测试用例（RED）
   - 实现功能迁移代码（GREEN）
   - 重构优化（IMPROVE）

3. **Diff 验证** — 每次迁移完成必须执行
   ```
   1. 对比老模块和新模块的对应逻辑
   2. 标注任何语义差异
   3. 修复差异直到等价
   ```

4. **更新 spec.md** — 使用 `/opsx:continue` 记录实现决策

### Phase 3: 验证与收尾

1. **一致性验证保障**

   ```
   必须完成的验证项：
   ✓ 复用老模块单元测试用例
   ✓ 搭建集成验证流程
   ✓ 覆盖正常、异常、边界全场景
   ```

2. **Code Review** — 使用 `superpowers:requesting-code-review`
   - 检查功能一致性
   - 验证数据填充逻辑

3. **风险与质量管控**
   - 预判隐式风险（全局状态、隐式依赖）
   - 制定回滚方案
   - 分步验证机制

## 功能迁移流程详解

### Step 1: 定位源功能

```
1. 在老模块中搜索目标功能
   └── Grep "func " + 功能名关键词

2. 找到功能入口方法
   └── LSP: goToDefinition 跳转到实现

3. 分析输入参数和返回值
   └── LSP: hover 查看结构体定义
```

### Step 2: 分析调用链路层级

```
1. 绘制调用层级图
   └── 入口 → 业务处理 → 子方法 → 工具函数

2. 识别每层的：
   - 方法签名
   - 入参/出参
   - 异常处理
   - 依赖关系

3. 标注隐式依赖（全局变量、side effects）
```

### Step 3: 分层迁移

```
1. 第一层：外层入口
   - 迁移 Controller/HTTP handler
   - 验证接口契约不变

2. 第二层：中层业务
   - 迁移 Service 层逻辑
   - 验证业务规则一致

3. 第三层：内层子方法
   - 迁移被调用的子方法
   - 验证数据转换逻辑

4. 第四层：底层工具
   - 迁移工具函数
   - 验证计算结果一致
```

### Step 4: 在新模块中实现

```
1. 遵循目标模块的架构风格
   ├── 保持现有 package 结构
   ├── 保持错误处理模式
   └── 保持接口定义风格

2. 移植业务逻辑
   └── 从老模块迁移到新模块
```

## 工具优先级详解

| 工具 | 适用场景 | 示例 |
|------|----------|------|
| LSP hover | 查看 struct 定义、类型信息 | `LSP: hover widgetsCtx` → 直接看到完整字段 |
| LSP goToDefinition | 跳转方法实现 | `LSP: goToDefinition GetXxxById` → 跳转数据访问入口 |
| LSP findReferences | 查找符号引用 | 查找某字段在哪些地方被使用 |
| Agent Explore | 未知区域探索（PHP/Go 混合） | 并行搜索多文件 |
| Grep | 精确文本匹配 | 备选方案 |
| Read | 已知路径文件 | 最后备选 |

## 强制规则

- [ ] Phase 1 每个步骤必须完成才能进入 Phase 2
- [ ] 未完成 brainstorming 禁止修改任何代码
- [ ] 未创建 OpenSpec 禁止进入实现阶段
- [ ] 迁移必须严格按"外层→中层→内层→底层"顺序执行
- [ ] 每层迁移后必须 Diff 验证，无差异才能进入下一层
- [ ] 禁止 AI 自主重构，必须逐行等价直译

## 验收标准

- [ ] 功能逻辑已迁移，与源模块行为一致
- [ ] 接口契约保持不变
- [ ] 遵循目标模块架构和风格
- [ ] 相关测试通过
- [ ] spec.md 已更新
- [ ] Code review 通过
- [ ] 全量回归测试通过

## 不要做

- 不要改变现有接口契约（除非明确要求）
- 不要直接复制老模块代码到新模块
- 不要破坏老模块的现有功能
- 不要忽略目标模块的架构风格
- 不要自主重构代码（禁止优化、禁止简化）
- 不要忽略隐式依赖（全局变量、side effects）
- 不要跳过 Diff 验证步骤
- 不要自动进行git提交
