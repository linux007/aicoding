# /migrate-feature - 代码功能迁移命令（精简版 v5.1）

> 目标：中型迁移（100-2000行）
> 改进：原子级任务 + 功能核对清单 + 强制 TDD + 记忆锚点

---

## 工作流程（5阶段）

### 阶段 1: 需求收集

**输入格式（必须按此格式提供）：**
```markdown
## 迁移任务
- 源: <源文件> (<具体功能/模块>)
- 目标: @<目标文件>
- 范围: <具体范围>

## 预设上下文（可选，减少探索时间）
- 入口函数签名: <如: func GetWidgetsConfig(...) (...)>
- 核心类型: <如: widgetsCtx{...}>
- 关键依赖: <如: thirdRPC.GetInfoByLessonId>
```

**示例：**
```markdown
## 迁移任务
- 源: teacherwidgets.go (GetWidgetsConfig及callback)
- 目标: @service/webplugin.go
- 范围: widgetscallbackMap + widgetnotplscallbackMap 全部实现

## 参考文件
- /Users/z004109/workhome/jxztgo/liveme/appvendor/teacherwidgets.go
```

**输入校验规则：**
| 字段 | 校验规则 | 不通过时的提示 |
|------|----------|----------------|
| 源 | 必须包含文件路径和功能描述，格式：`文件路径 (功能描述)` | ❌ 格式不正确，请使用：`源文件.go (具体功能模块)` |
| 目标 | 必须以 `@` 开头指向目标文件 | ❌ 目标文件必须以 `@` 开头，如：`@service/webplugin.go` |
| 范围 | 必须具体说明迁移范围，不能是"全部"这类模糊词 | ❌ 范围需具体，如：`funcA + funcB` 或 `widgetscallbackMap` |

**校验失败时的交互提示：**
```
⚠️  您的输入不符合迁移任务规范，请填写以下信息：

## 迁移任务
- 源: <源文件> (<具体功能/模块>)          ← 例如：teacherwidgets.go (GetWidgetsConfig及callback)
- 目标: @<目标文件>                       ← 例如：@service/webplugin.go
- 范围: <具体范围>                        ← 例如：widgetscallbackMap全部实现

## 预设上下文（可选）
- 入口函数签名: <如: func GetWidgetsConfig(...) (...)>
- 核心类型: <如: widgetsCtx{...}>
- 关键依赖: <如: thirdRPC.GetInfoByLessonId>
```

**校验通过条件：**
- ✅ 源字段匹配正则：`^\S+\.go\s*\([^)]+\)$`
- ✅ 目标字段以 `@` 开头且路径有效
- ✅ 范围字段包含具体功能名（非"全部"、"所有"等模糊词）

---

### 阶段 2: 探索代码库

**预设已知模式（通用框架）：**

如果用户已提供"预设上下文"，直接使用；否则按以下框架探索：

```markdown
## 入口函数模式
- 函数签名: 返回类型 + 参数列表
- 调用入口: main/public 函数

## 数据结构模式
- 核心类型: struct/interface 定义
- 组合关系: 类型间依赖

## 注册/回调模式
- 注册方式: map/registry/array
- 回调签名: 统一接口

## 依赖服务模式
- 外部调用: RPC/API/DB
- 注入方式: 参数/全局/单例
```

**探索要求：** 了解源/目标模块的代码风格和架构模式

---

### 阶段 3: 制定计划（核心）

`skill(name="superpowers:writing-plans")`

#### 功能核对清单
| # | 功能点 | 实现方式 | 测试用例 |
|---|--------|----------|----------|
| 1 | funcA 登录验证 | 迁移 | TEST-001 |
| 2 | funcB 密码加密 | 改用bcrypt | TEST-002 |

#### 任务清单
```markdown
- [ ] TASK-001: 迁移 funcA（关联 #1, TEST-001）
- [ ] TASK-002: 适配 funcB（关联 #2, TEST-002）
```
- 每任务 ≤ 50行变更
- 关联功能点和测试用例

---

### 阶段 4: TDD 实现

对每个任务：
1. 编写失败的测试（TEST-XXX）
2. 运行确认失败
3. 实现代码通过测试
4. 重构

**约束**：测试不过，不进入下一任务

---

### 阶段 5: 验收

```markdown
| # | 功能点 | 测试 | 结果 |
|---|--------|------|------|
| 1 | funcA | TEST-001 | ✅ |
| 2 | funcB | TEST-002 | ✅ |
```

**报告**：修改文件 + 功能核对结果 + 测试覆盖率

---

## 记忆锚点（重要）

探索完成后，必须输出以下结构化摘要供后续任务使用：

```markdown
### 代码结构摘要
- 入口函数: funcName() → 返回值
- callback 清单: [name1, name2, ...]
- 类型定义: TypeName{ field1, field2 }

### 依赖关系图
- funcA → depends on → [X, Y]
- funcB → provided by → Z

### 函数签名映射表（迁移前后一致性校验）
| 源函数 | 目标实现 | 参数一致 | 返回值一致 |
|--------|----------|----------|------------|
| funcA(a,b) (ret, err) | target.FuncA(a,b) (ret, err) | ✅ | ✅ |
| funcB(c) error | target.FuncB(c) error | ✅ | ✅ |

### 关键发现
- [发现1]
- [发现2]
```

---

## 约束

| 约束 | 说明 |
|------|------|
| 任务粒度 | ≤ 50行/任务 |
| 测试覆盖 | 每功能点必测 |
| TDD | 先写测试再实现 |
| 禁止 | 占位符、伪造API |

---

## 成功标准

- [ ] 功能核对清单 100% 覆盖源模块
- [ ] 所有测试通过
- [ ] 验收报告完整
